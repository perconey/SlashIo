@model IEnumerable<SlashIo.Models.PageDetails>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrentPrefix)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImagesAreUrlProvided)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentPrefix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImagesAreUrlProvided)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pagedetailsid }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pagedetailsid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pagedetailsid })
        </td>
    </tr>
}

</table>
